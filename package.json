{
  "name": "LocalTemperature",
  "description": "",
  "version": "0.0.0",
  "main": "main.js",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "start": "cd www && open http://localhost:8000 && python -m SimpleHTTPServer"
  },
  "dependencies": {
      "socket.io":"latest",
      "apn":"latest",
      "express":"latest"
  }
}
